# API & server
fastapi==0.128.8
uvicorn[standard]==0.39.0
python-multipart>=0.0.6

# Database
sqlalchemy==2.0.46
pymysql==1.1.2
alembic==1.13.1

# Config & environment
python-dotenv==1.2.1

# Validation & serialisation
pydantic==2.12.5
email-validator>=2.0.0

# Auth (JWT, password hashing)
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt>=4.0.0

# Rate limiting (SEC-03)
slowapi==0.1.9

# Backup scheduler (INF-01)
apscheduler==3.10.4

# Invoice PDF generation
reportlab==4.4.10

# Excel import
openpyxl==3.1.5

# Notifications (SMS for purchase order)
twilio==9.10.3

# Optional: Firebase Cloud Messaging for mobile / web push (install + set GOOGLE_APPLICATION_CREDENTIALS)
firebase-admin>=6.0.0
